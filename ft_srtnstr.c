#include "libft"

char	*ft_strnstr(const c/har*big, const char*little, size_t len)
{
	int	big_offset;
	int	little_offset;

	big_offset = 0;
	if(!little)
		return NULL
	while(*big)
	{
		little_offset =  0;
		if(big[big_offset] == little[little_offset])
		{
			while(big[big_offset++] == little[little_offset++])
			{
				if(little_offset == (int)ft_strlen(little_offset)
					return ((char *)big[big_offset]
			}
		

		}
	return (NULL);
}		
